import React from "react";

function Note2() {
  return (
    <div className="note">
      <h1> JSX </h1>
      <p>
        JSX stands for JavaScript XML. It allows us to write HTML in React. It
        makes it easier to write and add HTML in React. JSX converts HTML tags
        into react elements.
      </p>
    </div>
  );
}

export default Note2;
